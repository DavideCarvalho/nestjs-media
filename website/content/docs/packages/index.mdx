---
title: Overview
description: The full nestjs-media package set — core, the NestJS module, the browser client, disks, ORM stores, the image engine, and the ecosystem glue points.
---

Everything ships under the `@dudousxd/nestjs-media*` scope, versioned together. Install only what you use — disks, stores, and the image engine are separate packages so you never pull an S3 SDK or sharp into an app that doesn't need it.

## The map

| Package | Role |
|---|---|
| [`-core`](/docs/packages/core) | SPIs (`StorageDriver`, `MediaStore`, `ImageProcessor`), `StorageManager`, `MediaLibrary`, `uploadMode`, diagnostics, resumable-upload engine, tus handler |
| [`(umbrella)`](/docs/packages/nestjs) | NestJS `MediaModule` + `MediaService` + tus controller + the `/storage` subpath |
| [`-client`](/docs/packages/client) | framework-agnostic browser client — `uploadMedia` (tus) + `mediaUrl` |
| [`-disk-local`](/docs/packages/disk-local) | local filesystem driver |
| [`-disk-s3`](/docs/packages/disk-s3) | S3 driver (presign + native multipart) |
| [`-image-sharp`](/docs/packages/image-sharp) | sharp-backed `ImageProcessor` |
| [`-database-typeorm`](/docs/packages/database-typeorm) | TypeORM media store (non-destructive auto-schema) |
| [`-database-mikro-orm`](/docs/packages/database-mikro-orm) | MikroORM media store (safe auto-schema) |
| [`-database-drizzle`](/docs/packages/database-drizzle) | Drizzle media store (sqlite, migration-first) |
| [`-database-prisma`](/docs/packages/database-prisma) | Prisma media store (consumer-managed schema) |
| [`-telescope`](/docs/packages/telescope) | Telescope watcher consuming `nestjs:media:*` |
| [`-codegen`](/docs/packages/codegen) | Codegen extension emitting a typed media client |
| [`-react`](/docs/packages/react) | `useMediaUpload` hook + `MediaUploader` component |
| [`-testing`](/docs/packages/testing) | in-memory driver/store/upload-session + conformance suites |
| [`-dashboard`](/docs/packages/dashboard) | standalone `/media` console (SPA + JSON API) — disks, live uploads, media library |

## How they layer

```text
-react ─┐
        ├─▸ -client ──▸ (tus HTTP) ──▸ umbrella ─▸ -core
-codegen┘                                          │
                          -disk-local / -disk-s3 ──┤  (StorageDriver)
                          -image-sharp ────────────┤  (ImageProcessor)
   -database-{typeorm,mikro-orm,drizzle,prisma} ───┤  (MediaStore)
                          -telescope ──────────────┘  (diagnostics consumer)
```

`-core` defines the contracts; everything else either implements one (disks, stores, the image engine) or consumes the library (the umbrella module, the glue points). Start at [`-core`](/docs/packages/core) for the SPIs, or jump to the package you're wiring.
