---
"@dudousxd/nestjs-media": patch
---

Verification release for npm trusted publishing over OIDC, plus a comment cleanup in the tus client explaining why `uploadMedia()` skips the resume HEAD probe.
